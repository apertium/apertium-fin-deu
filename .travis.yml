dist: trusty
before_install:
    - wget http://apertium.projectjj.com/apt/install-nightly.sh -O - | sudo bash
    - sudo apt-get install hfst apertium lttoolbox apertium-dev apertium-deu lttoolbox-dev libhfst48-dev cg3
    - wget https://github.com/flammie/apertium-fin-deu/archive/master.zip
    - unzip apertium-fin-deu-master.zip
    - cd apertium-fin-deu-master && ./autoconf.sh && ./configure && make && make install
script:
    - ./autogen.sh
    - ./configure
    - make
    - make check
notifications:
    irc:
        channels:
            - "chat.freenode.net#omorfi"

